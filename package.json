{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "version": "0.11.2",
  "scripts": {
    "bootstrap": "lerna bootstrap --use-workspaces",
    "dev": "lerna exec yarn dev",
    "build": "lerna exec yarn build",
    "test": "lerna exec yarn test",
    "lint": "lerna exec yarn lint",
    "deploy": "yarn bootstrap && yarn build",
    "lerna:changelog": "git add CHANGELOG.md && git commit -m 'chore: CHANGELOG.md'",
    "lerna:patch": "changelog -p && yarn lerna:changelog && lerna version patch",
    "lerna:minor": "changelog -m && yarn lerna:changelog && lerna version minor",
    "lerna:major": "changelog -M && yarn lerna:changelog && lerna version major"
  },
  "devDependencies": {
    "lerna": "4.0.0",
    "standard-version": "^9.3.2"
  }
}
